<?xml version="1.0" encoding="utf-8"?>
<!--TODO 2: Setelah selesai pada build-gradle, maka berlanjut untuk melihat detail aplikasi pada AndroidManifest untuk melihat package,serta menargetkan activity pertama ke MainActivity karena hanya memiliki satu activity tetapi didalamnya terdapat banyak fragment -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.maylinda.cupcake">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cupcake"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>